# Эпик 2: Моделирование данных и хранилище

## ✅ Выполненные задачи

### 1. ORM-модели (app/infra/db/models.py)

Спроектированы и реализованы все основные ORM-модели с использованием SQLAlchemy 2.0+ и async поддержкой:

- **User** - пользователи системы
  - Telegram ID, username, имя
  - Флаги активности и администратора
  - Целевой чат для доставки
  - Пользовательские настройки (JSONB)
  
- **Source** - источники новостей
  - Telegram chat ID, название, username
  - Тип (channel/group/private)
  - Флаг активности
  - Метаданные (JSONB)
  
- **Subscription** - связь пользователей с источниками
  - User ↔ Source
  - Приоритет подписки
  - Флаг активности
  - Уникальная связь user_id + source_id
  
- **Filter** - фильтры для отбора сообщений
  - Режимы работы (keyword_only, semantic_only, combined)
  - Список ключевых слов (JSONB)
  - Список тем для семантики (JSONB)
  - Порог семантической схожести
  - Дополнительные настройки (JSONB)
  
- **Message** - исходные сообщения из источников
  - Telegram message ID и chat ID
  - Текст сообщения
  - Дата сообщения
  - Метаданные (медиа, ссылки, язык) (JSONB)
  - Флаг обработки
  - Уникальная связь telegram_message_id + chat_id
  
- **FilterMatch** - результаты применения фильтров
  - Ссылки на сообщение и фильтр
  - Тип совпадения (keyword/semantic/combined)
  - Оценка совпадения (score)
  - Детали совпадения (JSONB)
  - Уникальная связь message_id + filter_id
  
- **ForwardedMessage** - записи о доставке сообщений
  - Ссылки на пользователя, фильтр, сообщение
  - Целевой чат
  - ID пересланного сообщения в Telegram
  - Статус (pending/sent/failed)
  - Сообщение об ошибке
  - Временные метки создания и пересылки

**Особенности реализации:**
- Использование Mapped[] для type hints
- JSONB для гибких JSON-полей
- Enum типы для статусов и режимов
- Каскадное удаление (CASCADE)
- Индексы на важных полях
- Уникальные ограничения
- Relationships для связей между таблицами

### 2. Подключение к БД (app/infra/db/base.py)

Реализован модуль для работы с асинхронной БД:

- **DatabaseManager** - менеджер подключений
  - Создание async engine
  - Фабрика сессий
  - Инициализация БД (создание таблиц)
  - Закрытие соединений
  
- **Контекстные менеджеры**
  - `get_db_session()` - для standalone использования
  - `session()` - метод DatabaseManager
  - Автоматический commit/rollback
  
- **Dependency injection**
  - `get_session()` - для aiogram/FastAPI
  - Интеграция с DI фреймворками
  
- **Конфигурация**
  - Connection pooling (QueuePool/NullPool)
  - Echo режим для отладки
  - Настройки пула соединений
  - Автоматическая загрузка из settings

### 3. Репозитории (app/infra/db/repositories.py)

Реализован полный набор репозиториев с CRUD-операциями:

- **BaseRepository** - базовый класс с общими операциями
  - get(id) - получить по ID
  - get_all() - получить все с пагинацией
  - create(**kwargs) - создать новый
  - update(id, **kwargs) - обновить
  - delete(id) - удалить
  - count() - подсчитать количество

- **UserRepository** - операции с пользователями
  - get_by_telegram_id() - по Telegram ID
  - get_or_create_by_telegram_id() - получить или создать
  - get_active_users() - активные пользователи
  - get_admins() - администраторы
  - update_preferences() - обновить настройки

- **SourceRepository** - операции с источниками
  - get_by_telegram_chat_id() - по chat ID
  - get_or_create_by_telegram_chat_id() - получить или создать
  - get_active_sources() - активные источники
  - get_sources_by_type() - по типу (channel/group/private)

- **SubscriptionRepository** - операции с подписками
  - get_by_user_and_source() - по пользователю и источнику
  - get_user_subscriptions() - подписки пользователя
  - get_source_subscribers() - подписчики источника
  - create_subscription() - создать подписку
  - deactivate_subscription() - деактивировать

- **FilterRepository** - операции с фильтрами
  - get_user_filters() - фильтры пользователя
  - get_active_filters() - активные фильтры
  - update_keywords() - обновить ключевые слова
  - update_topics() - обновить темы
  - update_threshold() - обновить порог

- **MessageRepository** - операции с сообщениями
  - get_by_telegram_id() - по Telegram message ID
  - get_or_create_message() - получить или создать
  - get_source_messages() - сообщения из источника
  - get_unprocessed_messages() - необработанные
  - mark_as_processed() - отметить как обработанное
  - get_messages_by_date_range() - по диапазону дат

- **FilterMatchRepository** - операции с совпадениями
  - get_message_matches() - совпадения для сообщения
  - get_filter_matches() - совпадения фильтра
  - get_or_create_match() - получить или создать
  - get_top_matches() - топ совпадения по score

- **ForwardedMessageRepository** - операции с пересылками
  - get_user_forwarded_messages() - пересылки пользователя
  - get_pending_forwards() - ожидающие пересылки
  - mark_as_sent() - отметить как отправленное
  - mark_as_failed() - отметить как неудавшееся
  - get_message_forwards() - все пересылки сообщения
  - get_successful_forwards() - успешные пересылки

**Особенности репозиториев:**
- Использование selectinload для eager loading
- Типизация с Generic[ModelType]
- Async/await для всех операций
- Поддержка фильтрации и пагинации
- Автоматическое обновление updated_at

### 4. Миграции Alembic

Настроена система миграций:

- **Структура Alembic**
  - alembic.ini - конфигурация
  - alembic/env.py - окружение для async
  - alembic/script.py.mako - шаблон миграций
  - alembic/versions/ - директория миграций

- **Первая миграция** (001_initial_schema.py)
  - Создание всех таблиц
  - Создание индексов
  - Создание enum типов
  - Создание foreign keys с CASCADE
  - Создание уникальных ограничений
  - Downgrade функция для отката

- **Интеграция с настройками**
  - Автоматическая загрузка DSN из settings
  - Поддержка async engine
  - Сравнение типов и default значений

### 5. Вспомогательные скрипты

Созданы утилиты для работы с БД:

- **scripts/init_db.py** - инициализация БД
  - Создание таблиц
  - Опция --drop для пересоздания
  - Интерактивное подтверждение при drop
  
- **scripts/check_db.py** - проверка подключения
  - Проверка соединения
  - Список таблиц
  - Информация о столбцах
  - Вывод версии PostgreSQL

### 6. Примеры использования

- **examples/db_usage.py** - комплексный пример
  - Примеры для каждого репозитория
  - Создание тестовых данных
  - Демонстрация связей
  - Обработка транзакций

### 7. Документация

- **DOCS.md** - подробная документация
  - Описание схемы БД
  - Примеры работы с каждым репозиторием
  - Руководство по миграциям
  - Информация о транзакциях
  - Примеры тестирования

- **app/infra/db/__init__.py** - экспорты
  - Удобный импорт всех компонентов
  - Организованная структура экспортов

## 📁 Созданные файлы

```
app/infra/db/
├── __init__.py          # Экспорты модуля
├── models.py            # ORM модели (7 моделей)
├── base.py              # Подключение к БД
└── repositories.py      # Репозитории (7 репозиториев)

alembic/
├── versions/
│   └── 2024_12_16_1200-001_initial_schema.py
├── env.py               # Async окружение
└── script.py.mako       # Шаблон миграций

alembic.ini              # Конфигурация Alembic

scripts/
├── init_db.py           # Инициализация БД
└── check_db.py          # Проверка подключения

examples/
└── db_usage.py          # Примеры использования

DOCS.md                  # Документация по БД
EPIC2_SUMMARY.md         # Этот файл
```

## 🔧 Использование

### Инициализация БД

```bash
# Через Alembic
alembic upgrade head

# Через скрипт
python scripts/init_db.py

# С пересозданием таблиц (осторожно!)
python scripts/init_db.py --drop
```

### Проверка подключения

```bash
python scripts/check_db.py
```

### Примеры использования

```bash
python examples/db_usage.py
```

### В коде приложения

```python
from app.infra.db import (
    get_db_session,
    UserRepository,
    FilterRepository,
    MessageRepository,
)

async with get_db_session() as session:
    user_repo = UserRepository(session)
    user, created = await user_repo.get_or_create_by_telegram_id(
        telegram_id=123456789,
        username="user"
    )
```

## ✨ Ключевые особенности

1. **Полностью асинхронная архитектура** - все операции с БД async/await
2. **Типизация** - полная поддержка type hints и mypy
3. **JSONB поля** - гибкое хранение метаданных и настроек
4. **Relationships** - декларативные связи между таблицами
5. **Каскадное удаление** - автоматическая очистка связанных записей
6. **Индексы** - оптимизация запросов
7. **Уникальные ограничения** - предотвращение дублирования
8. **Enum типы** - типобезопасные статусы и режимы
9. **Миграции** - контролируемая эволюция схемы
10. **Репозитории** - чистая архитектура, изоляция БД логики

## 🎯 Готовность к следующим эпикам

Реализованная БД-инфраструктура полностью готова для:

- **Эпик 3**: Движок фильтрации - таблицы Filter, Message, FilterMatch
- **Эпик 4**: Семантический поиск - поля topics, semantic_threshold
- **Эпик 5**: Конвейер и диспетчер - репозитории для обработки сообщений
- **Эпик 6**: Юзер-бот - Source, Message для сохранения входящих сообщений
- **Эпик 7**: Control-бот - User, Filter, Subscription для управления

## 📊 Статистика

- **Модели**: 7 (User, Source, Subscription, Filter, Message, FilterMatch, ForwardedMessage)
- **Репозитории**: 7 + 1 базовый
- **Методов в репозиториях**: ~60+
- **Строк кода**: ~1800+
- **Enum типов**: 4 (SourceType, FilterMode, MatchType, ForwardedStatus)
- **JSONB поля**: 6 (preferences, metadata, keywords, topics, settings, details)

## 🚀 Следующие шаги

После завершения Эпика 2, можно переходить к:

1. **Эпик 3**: Движок фильтрации по ключевым словам
   - Использование Filter.keywords
   - Сохранение результатов в FilterMatch
   
2. **Эпик 4**: Семантический поиск
   - Использование Filter.topics и semantic_threshold
   - Расширение FilterMatch.details
   
3. **Эпик 5**: Конвейер и диспетчер
   - Использование всех репозиториев
   - Связывание Message → FilterMatch → ForwardedMessage
