# Telegram News Aggregator

Телеграм-бот для агрегации новостей с умной фильтрацией по ключевым словам и семантическому анализу.

## 📋 Описание

News Aggregator - это система, которая собирает сообщения из различных Telegram-каналов, групп и личных чатов, фильтрует их по заданным критериям и пересылает релевантные новости в целевой чат.

### Ключевые возможности

- **Двухбот архитектура**:
  - **User-bot** - читает сообщения из источников (каналы, группы, ЛС)
  - **Control-bot** - интерфейс управления для пользователей

- **Умная фильтрация**:
  - Фильтрация по ключевым словам (регистронезависимый поиск с нормализацией)
  - Семантическая фильтрация (поиск по смыслу с использованием эмбеддингов)
  - Комбинированные фильтры с настраиваемыми порогами

- **Гибкая настройка**:
  - Управление списком источников
  - Создание и редактирование фильтров
  - Настройка целевых чатов для доставки
  - Индивидуальные настройки для каждого пользователя

## 🏗️ Архитектура

Проект построен на основе асинхронного Python-стека:

```
app/
├── config/          # Конфигурация приложения
├── infra/           # Инфраструктурный слой
│   ├── db/         # База данных (SQLAlchemy)
│   └── logging/    # Логирование
├── domain/          # Доменная логика и сущности
├── filters/         # Движки фильтрации
│   ├── keyword_matcher.py
│   ├── semantic_matcher.py
│   └── pipeline.py
├── nlp/             # NLP обработка
│   ├── embeddings.py
│   └── preprocess.py
├── bots/            # Telegram боты
│   ├── user_bot/   # User-bot для чтения
│   └── control_bot/# Control-bot для управления
├── routing/         # Маршрутизация сообщений
└── tests/          # Тесты
```

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- PostgreSQL 14+
- Telegram Bot Token (для control-bot)
- Telegram API credentials (для user-bot)

### Установка

1. **Клонируйте репозиторий**:
```bash
git clone <repository-url>
cd NewsAggregator
```

2. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**:
```bash
cp .env.example .env
# Отредактируйте .env и заполните необходимые значения
```

5. **Настройте базу данных**:
```bash
# Создайте базу данных PostgreSQL
createdb news_aggregator

# Примените миграции (когда они будут готовы)
alembic upgrade head
```

### Конфигурация

#### Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен в `BOT_TOKEN`

#### Получение Telegram API Credentials

1. Перейдите на [my.telegram.org](https://my.telegram.org)
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте новое приложение
5. Скопируйте `api_id` и `api_hash` в соответствующие поля `.env`

### Запуск

```bash
# Запуск control-bot (управляющего бота)
python -m app.bots.control_bot.runner

# Запуск user-bot (агрегатора источников)
python -m app.bots.user_bot.runner
```

## 📝 Конфигурация

Все настройки осуществляются через переменные окружения в файле `.env`:

### Основные настройки

- `APP_ENVIRONMENT` - окружение (development/production/testing)
- `APP_DEBUG` - режим отладки
- `APP_TIMEZONE` - часовой пояс по умолчанию

### База данных

- `DB_HOST` - хост PostgreSQL
- `DB_PORT` - порт PostgreSQL
- `DB_USER` - пользователь БД
- `DB_PASSWORD` - пароль БД
- `DB_DATABASE` - имя базы данных

### Telegram боты

- `BOT_TOKEN` - токен control-бота
- `BOT_ADMIN_IDS` - список ID администраторов (через запятую)
- `USERBOT_API_ID` - API ID для user-бота
- `USERBOT_API_HASH` - API Hash для user-бота
- `USERBOT_PHONE` - номер телефона для user-бота

### Фильтрация

- `FILTER_ENABLE_KEYWORD` - включить фильтрацию по ключевым словам
- `FILTER_ENABLE_SEMANTIC` - включить семантическую фильтрацию
- `FILTER_SEMANTIC_THRESHOLD` - порог семантического сходства (0.0-1.0)
- `FILTER_EMBEDDING_MODEL` - модель для создания эмбеддингов

### Логирование

- `LOG_LEVEL` - уровень логирования (DEBUG/INFO/WARNING/ERROR/CRITICAL)
- `LOG_JSON_LOGS` - использовать JSON формат для логов
- `LOG_LOG_FILE` - путь к файлу логов

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием кода
pytest --cov=app --cov-report=html

# Запуск конкретного теста
pytest app/tests/unit/test_keyword_matcher.py
```

## 🔧 Разработка

### Форматирование кода

```bash
# Форматирование с помощью black
black app/

# Проверка с помощью ruff
ruff check app/
```

### Проверка типов

```bash
mypy app/
```

### Pre-commit hooks

```bash
# Установка pre-commit hooks
pre-commit install

# Запуск вручную
pre-commit run --all-files
```

## 📊 Основные сущности

- **User** - пользователь системы, взаимодействующий с control-ботом
- **Source** - источник новостей (канал, группа, ЛС)
- **Subscription** - подписка пользователя на источник
- **Filter** - набор правил для фильтрации сообщений
- **Message** - исходное сообщение из источника
- **FilterMatch** - результат применения фильтра к сообщению
- **ForwardedMessage** - запись о доставленном сообщении

## 🛣️ Roadmap

### ✅ Этап 1: Каркас проекта (текущий)
- [x] Структура проекта
- [x] Конфигурация (pydantic-settings)
- [x] Логирование

### 🔄 Этап 2: База данных
- [ ] ORM модели
- [ ] Репозитории
- [ ] Миграции (Alembic)

### 🔄 Этап 3: Фильтрация
- [ ] Keyword-фильтр
- [ ] Semantic-фильтр
- [ ] Конвейер фильтрации

### 🔄 Этап 4: Боты
- [ ] User-bot (Telethon)
- [ ] Control-bot (aiogram)
- [ ] Интеграция с фильтрами

### 🔄 Этап 5: Полировка
- [ ] Улучшенное логирование
- [ ] Метрики и мониторинг
- [ ] Тестовое покрытие 80%+
- [ ] Docker-контейнеризация

## 📄 Лицензия

MIT

## 🤝 Вклад в проект

Приветствуются pull requests. Для крупных изменений, пожалуйста, сначала откройте issue для обсуждения.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.
